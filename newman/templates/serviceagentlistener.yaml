apiVersion: v1
kind: Service
metadata:
  name: newman-server
  labels:
    {{- include "newman.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.listener.type }}
  ports:
    - port: {{ .Values.service.listener.port }}
      targetPort: https
      protocol: TCP
      name: https
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http  
  selector:
    {{- include "newman.selectorLabels" . | nindent 4 }}
